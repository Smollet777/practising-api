import { PercentsIntoTimePipe } from './percents-into-time.pipe';

describe('PercentsIntoTimePipe', () => {
  it('create an instance', () => {
    const pipe = new PercentsIntoTimePipe();
    expect(pipe).toBeTruthy();
  });
});
